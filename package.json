{
  "name": "plataforma-aprendizagem",
  "version": "2.0.0",
  "description": "Plataforma de aprendizagem com IA - cursos dinamicos gerados por LLM",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "cap:init": "npx cap init 'Aula de Inglês' com.auladeingles.app --web-dir public",
    "cap:add:android": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "android:build": "npm run cap:sync && cd android && ./gradlew assembleDebug",
    "android:run": "npx cap run android"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "express": "^4.18.2",
    "google-auth-library": "^10.6.2",
    "selfsigned": "^5.5.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  }
}

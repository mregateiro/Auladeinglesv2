{
  "name": "plataforma-aprendizagem",
  "version": "2.0.0",
  "description": "Plataforma de aprendizagem com IA - Android app (Capacitor)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "cap:init": "npx cap add android",
    "cap:sync": "npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:build": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "cap:build-release": "npx cap sync android && cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^7.2.0",
    "@capacitor/core": "^7.2.0",
    "cookie-parser": "^1.4.6",
    "express": "^4.18.2",
    "google-auth-library": "^10.6.2",
    "selfsigned": "^5.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.2.0"
  }
}
